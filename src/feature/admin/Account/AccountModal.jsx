// import { Modal } from "antd";
// import AccountForm from "./AccountForm";

// const AccountModal = ({ visible, onCancel, onSubmit, initialValues = {} }) => {
//     const isNew = Object.keys(initialValues).length === 0;

//     return (
//         <Modal
//             title={isNew ? "Thêm tài khoản" : "Cập nhật tài khoản"}
//             open={visible}
//             onCancel={onCancel}
//             footer={null}
//             destroyOnClose={true} 
//         >
//             <AccountForm initialValues={initialValues} onSubmit={onSubmit} isNew={isNew} />
//         </Modal>
//     );
// };

// export default AccountModal;
