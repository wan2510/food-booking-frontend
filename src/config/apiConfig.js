export const API_CONFIG = {
    USE_MOCK_API: false, // Có thể thay đổi true/false để bật tắt mockApi
    API_BASE_URL: 'http://localhost:8080/api',
    ENDPOINTS: {
        TABLES: '/tables'
    }
};
